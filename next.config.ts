import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['encrypted-tbn0.gstatic.com', 'img.freepik.com', 'avatars.mds.yandex.net','image.winudf.com'], 
  },
};

export default nextConfig;
